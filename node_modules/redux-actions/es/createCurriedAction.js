import createAction from './createAction';
import curryN from 'lodash-es/fp/curryN';

export default (function (type, payloadCreator) {
    return curryN(payloadCreator.length, createAction(type, payloadCreator));
});